#! scanner-scanner_data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: scanner-scanner-data
    namespace: api-auth-scanner
    labels:
        com.docker.compose.service: scanner
        com.docker.compose.volume: scanner_data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
